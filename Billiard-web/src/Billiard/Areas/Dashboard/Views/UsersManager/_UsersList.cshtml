@model PagedUsersListViewModel

@{
    ViewData["Title"] = "مدیریت کاربران";
}

<partial name="~/Areas/Dashboard/Views/UsersManager/_SearchUsers.cshtml" model="new SearchUsersViewModel()" />

@if (Model.Users.Any())
{
    foreach (var user in Model.Users)
    {
        <div id="User-@user.Id">
            <partial name="~/Areas/Dashboard/Views/User/_UserMngItem.cshtml"
                     model="new UserItemViewModel { User = user, ShowAdminParts = true, Roles = Model.Roles, ActiveTab = UserItemActiveTab.UserInfo }" />
        </div>
    }
}
else
{
    <div class="alert alert-warning">کاربری یافت نشد.</div>
}

<div>
    <cs-pager cs-paging-pagesize="@Model.Paging.ItemsPerPage"
              cs-paging-pagenumber="@Model.Paging.CurrentPage"
              cs-paging-totalitems="@Model.Paging.TotalItems"
              cs-pagenumber-param="page"
              asp-area="@AreaConstants.DashboardArea"
              asp-controller="UsersManager"
              asp-action="Index"
              cs-show-first-last="true"
              cs-suppress-empty-nextprev="true"
              cs-suppress-inactive-firstlast="true"
              cs-first-page-text="ابتدا"
              cs-last-page-text="انتها"
              cs-pager-li-current-class="active"
              cs-pager-li-non-active-class="disabled"
              cs-pager-ul-class="pagination"
              cs-pager-li-current-class="page-item active"
              cs-pager-li-other-class="page-item"
              cs-pager-li-non-active-class="page-item disabled"
              cs-pager-link-current-class="page-link"
              cs-pager-link-other-class="page-link"></cs-pager>
</div>